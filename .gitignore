# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Python
__pycache__
*.pyc
.venv
*.egg-info

# Environment files with secrets (track .env.example instead)
.env
backend/.env

# BAML generated client (regenerate with: baml-cli generate)
#baml_client/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.env.local

# Coverage output (vitest --coverage)
coverage/

# Generated: a headless capability-registry dump (`frontend_version: "headless-dump"`), used
# for bindings verification. It was TRACKED BY ACCIDENT — swept into 2a3c006 by a `git add -A`
# — and a generated probe output in version control is a file every lane will keep dirtying and
# nobody owns. Kept on disk, out of the index.
caps_dump.json
