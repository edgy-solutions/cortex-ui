# ── Frontend Environment ──────────────────────────────────
# This file is read by Vite. ONLY VITE_* vars are bundled into client JS.
# NEVER put secrets or internal service URLs here.
VITE_API_URL=http://localhost:8000
